# requirements solo para el servidor (sin Kivy, más liviano para deploy)
pillow>=10.0.0
requests>=2.31.0

# ML — torch ya se instala en el Dockerfile (CPU-only)
# No incluir torch/torchvision aquí para evitar reinstalación

# Server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
